name: functional-test

on:
  workflow_dispatch:
  workflow_call:

jobs:
  functional-test:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run frontend functional tests
        run: |
          node test/frontend/frontend-behavior.test.js

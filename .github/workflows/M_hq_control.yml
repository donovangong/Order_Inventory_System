name: HQ Control of All workflow

on:
  #push:
  #  branches: [ "main" ]
  workflow_dispatch:

jobs:
    build:
        name: 1_Build 
        uses: ./.github/workflows/1_build.yml

    
    functional_test:
        name: 2_Functional_Test
        uses: ./.github/workflows/2_functional-test.yml
        needs: build


    deploy:
        name: 4_Deploy
        uses: ./.github/workflows/3_deploy.yml
        needs: functional_test
    
